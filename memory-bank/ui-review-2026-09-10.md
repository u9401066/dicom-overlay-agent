# Report readability repair — isolated candidate, 2026-09-10

Branch `agent/astra-ui-report-20260910` is separate from the frozen real Astra
GUI cohort. Do not merge its source into the running cohort worktree midway.

Actual app-owned report images showed two defects: verbose review warnings
buried findings, and Qt compressed long word-wrapped labels below their required
height, clipping clinical text. The candidate preserves all clinical content,
review reasons/citations, source findings and coordinates, while:

- showing a compact, keyboard-operable review alert before findings and full
  selectable reasons after them;
- stably prioritizing critical/warning findings for display only;
- separating high-contrast text colors from translucent bbox colors;
- enforcing actual width-dependent text height on resize/text/font/style changes;
- clearing stale incomplete/zoom/review messages and resetting report scroll.

A real Windows panel preview rendered a saved private report outside the active
Viewer ROI; no inference or image upload occurred. After repair, measured label
heights were 311/263/135 logical pixels, matching their `heightForWidth` values.
The earlier 207/206/135 allocation clipped the first two findings. The final
preview was visually inspected; it is UI regression evidence, not a new model
case, packaged acceptance, or clinical accuracy result.

Validation: **41 targeted geometry/capture/interaction tests passed**; the final
isolated full suite passed **1241 tests, 5 explicit skips, 171.54 seconds**.
Ruff passed and the changed presentation module passed mypy. The isolated suite
does not contain the ignored real cohort fixture or a newly built executable;
those skips are not clinical or frozen-binary acceptance. The initial run lacked the pinned
OpenClaw/portable Node files, causing five native plugin import failures; one
local socket test also failed transiently and passed on recheck. Runtime files
were then copied without config, auth, or state for the successful isolated rerun.

No new runtime dependency, model prompt, schema, ROI rule, box mapping, or
OpenClaw transport change is part of this UI repair.
